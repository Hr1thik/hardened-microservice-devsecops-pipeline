{
  "name": "devsecops-lab-app",
  "version": "1.0.0",
  "description": "Hardened Node.js microservice for DevSecOps demonstration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}